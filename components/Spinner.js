import { BeatLoader, CircleLoader, DotLoader, HashLoader, PuffLoader, RingLoader } from "react-spinners";

export default function Spinner() {
    return(
        <RingLoader color={'#36d7b7'} speedMultiplier={2}/>
    );
}